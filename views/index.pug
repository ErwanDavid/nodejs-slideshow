html
  head
    title= title
    script(src="/socket.io/socket.io.js")
    script.
        var socket = io.connect("#{socketAddress}");
        socket.on('reload', function (data) {
            location.reload();
        });

  body(style={width:'100%', height:'100%', margin:'0'})
    img(src=currentFileSrc style={width:'100%', height:'100%'})
